# GelDoc

Pipeline проекта:

## [1] Для обучения YOLO:

1) Берём изображения и поворачиваем их, **используя CV2**, чтобы все (не понятно, как работает метод) объекты были горизонтальными
2) Сохраняем повёрнутые изображения в отдельную папку
3) Берём их аннотации и пересчитываем все значения исходя из поворота
     **ВНИМАНИЕ: надо подумать, что использовать для поворота аннотаций: rotation из аннотаций или значение, полученное через CV2** 
4) Сохраняем новые аннотации в отдельный файл
5) Обучаем YOLO на повёрнутых изображениях и повёрнутых аннотациях к ним

## [2] Для обучения Decider:

1) Берём изображения и поворачиваем их, **используя CV2**, чтобы все (не понятно, как работает метод) объекты были горизонтальными
2) Сохраняем повёрнутые изображения в отдельную папку
3) Применяем обученную YOLO на повёрнутых изображениях
4) Применяем Classic algorithm на повёрнутых изображениях
5) Формируем DataFrame из полученных данных
6) Формируем вектор целевой переменной (**КАК???**)
7) Обучаем Decider на полученном DataFrame

## [3] Для применения:

1) Берём изображение и поворачиваем его, **используя CV2**, чтобы все (не понятно, как работает метод) объекты были горизонтальными
2) Сохраняем его повернутую версию
3) Применяем YOLO на повёрнутом изображении
4) Применяем Classic algorithm на повёрнутом изображении
5) Формируем DataFrame из полученных данных
6) Применяем Decider
